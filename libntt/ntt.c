#include <assert.h>
#include "ntt.h"

char * ntt_list_testcases(const char * path) {
	assert(path != NULL);

	return NULL;
}
char * ntt_list_imports(const char * path) {
	assert(path != NULL);

	return NULL;
}


